# instalar python 3.8 desde la pagina oficial
# Abrir el explorador de archivos, ir a la ruta donde quuiere guardar el repsositorio
# Click derecho en una zona vacia dentro de la carpeta > mas opciones > abrir git bash
# En la terminal de git:
# git init
# git clone "copiar el link que sale en la parte de CODE desde el repositorio de github "
# Abrir Visual Studio Code
# File > Open Folder > "Carpeta donde se guardo el repositorio"
Terminal > New Terminal
# pip install virtualenv
# virtualenv -p python3.8 env
# cd env
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
# ./Scripts/activate
# pip install -r "Ruta_archivo_requerimientos.txt"

